# Add subdirectories here if exist

# ~

file(GLOB NB_HEADERS_IN_CURRENT_DIR "${CMAKE_CURRENT_LIST_DIR}/*.h")
file(GLOB NB_SOURCES_IN_CURRENT_DIR "${CMAKE_CURRENT_LIST_DIR}/*.hpp" "${CMAKE_CURRENT_LIST_DIR}/*.cpp")
                                    
set(NB_CURRENT_BUILD_HEADERS ${NB_HEADERS_IN_CURRENT_DIR} ${NB_CURRENT_BUILD_HEADERS} PARENT_SCOPE)
set(NB_CURRENT_BUILD_SOURCES ${NB_SOURCES_IN_CURRENT_DIR} ${NB_CURRENT_BUILD_SOURCES} PARENT_SCOPE)